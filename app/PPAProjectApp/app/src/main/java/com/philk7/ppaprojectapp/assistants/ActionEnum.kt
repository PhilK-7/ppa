package com.philk7.ppaprojectapp.assistants

/**
 * This enum contains an element for every (abstract) method in the interface IPrivacyAssistant.
 * It represents generic actions that are implemented by every privacy assistant.
 */
enum class ActionEnum {
    INIT,
    PRIVACY_QUERY,
    LOCATION_QUERY
}